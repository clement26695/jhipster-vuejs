import 'cypress-file-upload';
import './authentication';
import './navbar';
import './utils';
<%_ if (authenticationType !== 'oauth2') { _%>
import './users';
<%_ } _%>
/* jhipster-needle-add-support-file - JHipster will add Cypress support files here */
<%_ if (authenticationType === 'oauth2') { _%>

Cypress.Cookies.defaults({
  whitelist: ['JSESSIONID', 'XSRF-TOKEN', 'AUTH_SESSION_ID', 'KEYCLOAK_IDENTITY', 'KEYCLOAK_SESSION']
})
<%_ } else if (authenticationType === 'session') { _%>

Cypress.Cookies.defaults({
  whitelist: ['XSRF-TOKEN']
});
<%_ } // TODO: add for uaa _%>
